import { Text } from "react-native";
import { ThemeView } from "../../components/ThemeView";

export const OutfitLog = () => {
  return (
    <ThemeView>
      <Text className="text-4xl">Outfit Log</Text>
    </ThemeView>
  );
};
